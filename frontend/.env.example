# API Configuration (Optional - Frontend works standalone with dummy data)
# Only needed if you want to connect to a backend API
VITE_API_BASE_URL=

# For production deployment on Netlify/Vercel, leave empty to use frontend-only mode
# Example: VITE_API_BASE_URL=https://your-backend-api.com/api
